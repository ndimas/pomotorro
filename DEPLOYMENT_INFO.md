# Deployment Information

This project is configured for automatic deployment.

**IMPORTANT:** Any changes merged to the `master` branch are automatically pushed to production on **Cloudflare**.

Please ensure all code is reviewed and tested locally before merging to `master`.
